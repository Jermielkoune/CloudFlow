# ---------------------------------------------------------------------------
# Template : Tests d'Intégration (Testcontainers)
# Objectif : Valider le code sur une vraie base de données conteneurisée
# ---------------------------------------------------------------------------
#parameters:
#  - name: workingDir
#    type: string
#  - name: mavenCacheFolder
#    type: string
#    default: $(Pipeline.Workspace)/.m2/repository
#
#steps:
#  - bash: |
#      echo "--- Vérification du démon Docker ---"
#      docker info
#    displayName: 'Check Docker Status'
#
#  - bash: |
#      set -euo pipefail
#      echo "--- Lancement des Tests d'Intégration (Testcontainers) ---"
#      cd "${{ parameters.workingDir }}"
#
#      ./mvnw -B -ntp verify \
#        -Dsurefire.skip=true \
#        -Dspring.profiles.active=testcontainers \
#        -Dmaven.repo.local=${{ parameters.mavenCacheFolder }}
#    displayName: 'Run Integration Tests (Testcontainers)'
#
#  - task: PublishTestResults@2
#    displayName: 'Publish IT Results'
#    inputs:
#      testResultsFormat: 'JUnit'
#      testResultsFiles: '**/target/failsafe-reports/TEST-*.xml'
#      searchFolder: '${{ parameters.workingDir }}'
#      failTaskOnFailedTests: true
#    condition: always()
